version: "3.9"

services:
  ppe-detection:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./output:/app/output
      - ./logs:/app/logs
      - ./input:/app/input
    environment:
      - PPE_API_HOST=0.0.0.0
      - PPE_API_PORT=8000
